[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "laofu-browser"
version = "0.1.0.dev1"
description = "Python client for the laofu-browser HTTP service"
requires-python = ">=3.10"
dependencies = []
[tool.setuptools.packages.find]
where = ["."]
include = ["laofu_browser*"]
