# X 读取现场验收

**简体中文** | [English](X_READ_ACCEPTANCE.en.md)

2026-09-15，22:55–23:04 UTC。**基础读取已实测可用；列表完整读取未通过，存在屏幕外帖子遗漏。** 本次直接调用 laofu-browser API 和配对扩展，使用已登录的日常 Chrome、新建独立测试标签页；没有借用其他浏览器工具完成读取。

用户后续决定：X 列表问题暂不处理，接受当前基础读取能力。X-READ-01 保留为已知限制，不作为公开预览版的阻塞项；该决定不改变列表完整性未通过的测试结果，也不构成已公开仓库或发布制品的声明。

## 环境与结果

实际服务为 `0.1.0-dev.2`，构建时间 `2026-09-15T08:58:26.708Z`，提交 `592b5c97530c3c716909fb3cda66bf6f5dceece7`、`dirty=true`。**该日常运行环境尚未升级到最新 28 项对抗缺陷修复版**；以下结论不能当作最新候选验收。

|场景|现场结果|判断|
|---|---|---|
|公开主页 `x.com/OpenAI`|可读取帖子正文；DOM 可取得作者、时间、帖子链接与图片数量。滚动后观察到 12 个此前未出现的帖子 ID，正文读取正常返回|基础读取通过；不是全历史采集|
|单帖详情 `2097786616311840853`|主页折叠正文为 276 字符；详情展开后，可见正文 428 字符与 `read_text` 一致，无“Show more”|主帖读取通过；未完整展开全部回复|
|搜索 `from:OpenAI`，Latest|初始 6 条帖子正文均能与 `read_text` 对上|基础搜索读取通过|
|搜索滚动续读|滚动后即时观察到 5 个新帖子 ID；随后 DOM 有 12 条，正文输出含其中 11 条|续读可用，完整性不通过|
|遗漏帖的单独详情 `2097374646148481532`|详情可见正文 299 字符完整匹配，已展开|单帖补读通过|
|快照与滚动返回|1 次快照、2 次滚动返回 `partial`、`truncated=true`、`effectState=confirmed`；后续 DOM 证明滚动生效|正确保留截断状态，不能算完整快照|

字符数按 JavaScript 字符串长度计；匹配忽略排版空白与零宽字符，并排除站点标记为 `aria-hidden` 的链接辅助文字。页面帖子数量会随异步加载与虚拟列表变化，表中不同阶段的数量不代表固定分页大小。

## X-READ-01：列表输出存在未标记的遗漏

已复现于上述已安装环境：搜索页 DOM 中存在公开帖子 `https://x.com/OpenAI/status/2097374646148481532`，位于屏幕外；两次 text 读取及一次 Markdown 读取都未包含它，均返回 `succeeded`、`truncated=false`，匹配结果都是 **11/12**。

诊断时，同一次 `eval` 中读取该帖 DOM，并按当前 `mainText` 的容器与清理规则复制主容器，复制结果仍包含该帖。因此，不能仅用“尚未加载”或“ARIA 隐藏文字”解释这一次遗漏。实际 API 输出与 DOM 的差异已确认；扩展执行上下文、输出路径或页面变化中的具体根因尚未定位，最新修复候选也未复验。单帖详情读取成功，不能据此消除列表缺项。

另外，两次详情页检查分别有 2 条、1 条屏幕外回复未出现在通用正文输出中；主帖均完整匹配。它们作为相关现象保留，未宣称已经定位为同一根因。

当前可行用法是取得帖子永久链接后逐帖打开读取；若要批量采集，仍需按帖子 ID 去重、逐帖补读并验证覆盖范围。本次没有实现或验收自动全量采集流程。

## 证据与范围

- [脱敏摘要与原始回执哈希](evidence/x-read/summary.json)：30 次命令，27 次 `succeeded`、3 次 `partial`；这只是命令状态统计，不是 30 个用例全部通过。
- 原始回执与测试脚本留在忽略目录 `workspace/x-read-live/`；回执文件权限为 `0600`，可能包含页面侧栏信息，不纳入仓库。公开摘要只保留状态、公开帖子链接、数量和哈希。
- 最初测试脚本将 `partial` 一律当作失败退出，后续改为保留其截断状态并继续只读核验；没有重新执行已确认生效的滚动来掩盖该记录。
- 全程只读取公开内容，没有发帖、回复、点赞、关注或读取私信。本次没有遇到要求人工处理的登录或验证码页面；不能据此推断其他网络环境的风控结果。
- 已关闭本次新建标签页并删除测试会话；保留日常 Chrome、原有标签页和登录状态。
- 未测试未登录模式、日本 VPS、全历史、所有回复、图片／视频下载或 `capture` 图文包导出；未升级扩展、推送或发布。P4 生命周期验收和最终冻结仍按当前状态入口跟踪。
