diff --git a/src/worker.ts b/src/worker.ts
index 280c5da..d2da71b 100644
--- a/src/worker.ts
+++ b/src/worker.ts
@@ -575,6 +575,13 @@ export class Worker {
       return;
     }
     const dir = mkdir(path.join(this.config.home, "jobs", job.id));
+    let clientRetired = false;
+    const retireClient = async () => {
+      if (!job.sessionId && !clientRetired) {
+        await this.browser.releaseClient(job.id);
+        clientRetired = true;
+      }
+    };
     try {
       if (
         job.type === "browser.flow@v1" &&
@@ -826,6 +833,8 @@ export class Worker {
               : "none",
         },
       };
+      // Retire before advertising a reusable worker to the broker.
+      await retireClient();
       const response = {
         type: "result",
         jobId: job.id,
@@ -858,6 +867,8 @@ export class Worker {
         stopped = false;
       });
       const error = problem(e);
+      // Retire before advertising a reusable worker to the broker.
+      await retireClient();
       const response = {
         type: "result",
         jobId: job.id,
@@ -872,7 +883,7 @@ export class Worker {
     } finally {
       // Task-only clients never outlive the job. Persistent sessions are retired
       // by session_closed, idle expiry, capacity eviction, or worker shutdown.
-      if (!job.sessionId) await this.browser.releaseClient(job.id);
+      await retireClient();
       fs.rmSync(dir, { recursive: true, force: true });
       this.running = false;
       this.active = undefined;
diff --git a/test/adversarial-interfaces.test.ts b/test/adversarial-interfaces.test.ts
index 8bda196..3dc6ce1 100644
--- a/test/adversarial-interfaces.test.ts
+++ b/test/adversarial-interfaces.test.ts
@@ -134,6 +134,7 @@ test("error summary flags truncation while preserving the full original tool con
   const text = "x".repeat(1800);
   let result: any;
   w.browser = {
+    releaseClient: async () => {},
     control: async () => {},
     call: async () => ({
       isError: true,
diff --git a/test/adversarial.test.ts b/test/adversarial.test.ts
index 3105bdc..013df1a 100644
--- a/test/adversarial.test.ts
+++ b/test/adversarial.test.ts
@@ -274,6 +274,7 @@ test("failed worker execution cleans only its own temporary job directory", asyn
   });
   t.after(() => w.store.close());
   w.browser = {
+    releaseClient: async () => {},
     control: async () => {},
     call: async () => {
       throw Error("fixture failure");
@@ -298,6 +299,7 @@ test("interrupted act after completed steps remains unknown and cannot report a
   t.after(() => w.store.close());
   let response: any;
   w.browser = {
+    releaseClient: async () => {},
     control: async () => {},
     call: async () => ({
       content: [],
diff --git a/test/client-lifecycle.test.ts b/test/client-lifecycle.test.ts
index d80a7f3..6fb57d6 100644
--- a/test/client-lifecycle.test.ts
+++ b/test/client-lifecycle.test.ts
@@ -50,3 +50,30 @@ test("closing a Session notifies its Worker after denying new commands", async (
     assert.equal(denied.statusCode, 403);
   } finally { await server.app.close(); fs.rmSync(home, { recursive: true, force: true }); }
 });
+
+for (const fails of [false, true]) {
+  test(`task completion waits for client retirement (fails=${fails})`, async () => {
+    const home = fs.mkdtempSync(path.join(os.tmpdir(), "lb-client-drain-"));
+    const w: any = new Worker({ home, profileId: "fixture", workerId: "fixture", token: "fixture", baseUrl: "http://unused" });
+    let release!: () => void, entered!: () => void;
+    const gate = new Promise<void>((r) => { release = r; });
+    const cleanupStarted = new Promise<void>((r) => { entered = r; });
+    const sent: any[] = [];
+    w.browser = {
+      control: async () => {},
+      call: async () => { if (fails) throw Error("fixture failure"); return { content: [] }; },
+      releaseClient: async () => { entered(); await gate; }, diagnostic() {},
+    };
+    w.send = (m: any) => sent.push(m);
+    const execution = w.execute({ id: id("tsk"), sessionId: "", kind: "task", type: "browser.flow@v1",
+      attempt: 1, fence: 1, expiresAt: Date.now() + 900_000,
+      input: { steps: [{ tool: "read_text", args: {} }] } });
+    try {
+      await cleanupStarted;
+      assert.equal(sent.some((m) => m.type === "result"), false);
+      release(); await execution;
+      assert.equal(sent.find((m) => m.type === "result").state, fails ? "failed" : "succeeded");
+      assert.equal(w.running, false);
+    } finally { release(); await execution; w.store.close(); fs.rmSync(home, { recursive: true, force: true }); }
+  });
+}
